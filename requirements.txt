# ============================================
# AvoidMaker 项目依赖 (CPU 版本)
# ============================================
# 安装方式:
#   pip install -r requirements.txt
#
# 注意: PyTorch需要单独安装CPU版本，请先运行:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# ============================================

# --- 核心依赖 ---
numpy>=1.21.0
gymnasium>=0.28.0
pygame>=2.1.0

# --- 深度学习 (CPU版本需单独安装，见上方说明) ---
# torch  # 请单独安装CPU版本

# --- 凸优化 (CBF控制器) ---
cvxpy>=1.3.0
osqp>=0.6.0

# --- 分布式训练 ---
ray>=2.0.0

# --- 日志与可视化 ---
tensorboard>=2.10.0
Pillow>=9.0.0

# --- 性能加速 (可选但推荐) ---
numba>=0.56.0

# --- 其他可选依赖 ---
# wandb  # 实验追踪 (可选)
# setproctitle  # 进程标题 (可选)
